# Scripts

This directory only contains scripts that are not central to the functionality of the app.

## Usage

As a convention, all scripts should only be executed from the root of the repository, not from
the scripts directory itself. This prevents path reference issues and makes everybody's life easier.

For an example if you would like to run the remote installation script installing the latest
version from Codeberg, you may run `scripts/remote_install.sh`.
